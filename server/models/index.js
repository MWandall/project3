// thinking about what models are needed and what attributes those will in turn need.
// basics are probably user battle and monster models. We could possibly combine those down into 2 models. 
// 

//TODO require the other model js files inside of thise index file so we can require this entire folder elsewhere.